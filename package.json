{
  "name": "@agynio/cmr-memory",
  "version": "1.0.2",
  "description": "Persistent memory for Claude Code agents",
  "license": "MIT",
  "main": "dist/cli.js",
  "bin": {
    "cmr-memory": "bin/cmr-memory"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "npm exec --yes --package typescript@5.8.3 -- tsc",
    "prepare": "npm run build",
    "lint": "tsc --noEmit",
    "test": "npm run build && node --test --test-concurrency=1"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "openai": "^4.104.0",
    "uuid": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.8.3"
  }
}
